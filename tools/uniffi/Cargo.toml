[package]
name = "pessimal_uniffi_bindgen"
description = "UniFFI bindgen CLI, version-locked to the workspace uniffi dependency."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "pessimal-uniffi-bindgen"
path = "uniffi_bindgen_main.rs"

[dependencies]
uniffi = { workspace = true, features = ["cli"] }

[lints]
workspace = true
